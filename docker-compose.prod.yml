version: '3.8'

services:
  web:
    build: ./src
    command: gunicorn --bind 0.0.0.0:5000 manage:APP
    volumes:
      - ./src/:/usr/app/
    ports:
      - 5000:5000
    env_file:
      - ./.env.prod
