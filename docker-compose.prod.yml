version: '3.8'

services:
  web_api:
    build: ./src
    command: gunicorn --bind 0.0.0.0:5000 manage:APP
    volumes:
      - ./src/:/usr/app/
    expose:
      - 5000
    env_file:
      - ./.env.prod

  nginx:
    build: ./nginx
    ports:
      - 1337:80
    depends_on:
      - web_api
